using UnityEngine;
using System.Collections;

public class PluginEditorScript : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	    var type = typeof (PluginScript);
//        var type1 = typeof(Script);
    }

    // Update is called once per frame
    void Update () {
	
	}
}
