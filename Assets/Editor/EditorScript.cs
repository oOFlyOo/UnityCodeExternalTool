using UnityEngine;
using System.Collections;

public class EditorScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        var type = typeof(PluginScript);
        var type1 = typeof(PluginEditorScript);
        var type2 = typeof(Script);
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
